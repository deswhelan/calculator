//Create array to store calculator inputs
var inputs = [];
var total = 0;

//Create a variable to store current button value
var temp = "";

//run the script when any button is clicked

    //get the value of the clicked button

    //If it is a number, assign it to temp

    //If it is AC then re-initialise all variables

    //Then add current symbol to the inputs array and clear temp
    //Translate multiply and divide symobl to mathematical values

    //If it is equals then perform the calculation

        //Use a loop to iterate over each value of the inputs array

            //initialise variables to use in calculations then..
            //add
            //subtract
            //multiply
            //divide
    
        //"Swap the '-' symbol so text input handles it correctly"; Need to figure out what this means!

        //Declare the answer and clear the variables
        function displayAnswer () {
            var testAnswer = 80085;
            document.getElementById("display").innerHTML = testAnswer;
        }

        

    //Otherwise add temp and val to inputs and clear temp


        