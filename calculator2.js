//Create array to store calculator inputs
var inputs = [];
var total = 0;

//Create a variable to store current button value
var temp = "";



 //Store values of clicked buttons to inputs array
 function storeClickedButton (buttonValue) {
    inputs.push(buttonValue);
    console.log(inputs);
}

